# みなの社会保険労務士事務所HP — 作業ルール

## 1. このサイトの正体

- ビルド工程のない静的HTML／CSS／JavaScriptサイト。Reactは使っていない。
- 公開内容の正本はリポジトリのルート直下。バックアップ、書き出し物、古い作業ツリーを正本にしない。
- `uploads/` は別CSS系統で、ルート系の規則が自動では届かない。
- `.github/` はWeb本文として処理・削除しない。公開基盤そのものを変更する依頼以外では編集しない。
- プレビュー環境の空の `#root` 警告は、この静的サイトでは無害。

## 2. 正本の優先順位

| 優先 | 用途 | 正本 |
|---|---|---|
| 最優先 | 現行の表示・挙動・値 | `origin/main` のコード、manifest、検査・生成スクリプトと本番実測 |
| 次 | 現在の変更手順 | `docs/` 直下の手順書 |
| 次 | 事業方針・公開物の判断 | みなの事務所の開業支援コンテキストと、そこから参照される公開HP基準 |
| 参考 | 過去の事故と採否理由 | `docs/incidents/`。現行仕様の根拠にせず、再発防止のために読む |

法令、料率、期限、料金、製品機能は、公開時点の公式一次資料をコードより優先して再確認する。矛盾や未確定情報を推測で埋めない。

## 3. 着手前の分類

- **分析のみ**：読み取りと原因報告まで。ファイル、GitHub、本番を変更しない。
- **局所修正**：対象要素、適用元、直接の同期先だけを調べ、対象ページ・対象幅で検証する。
- **新規作成**：既存テンプレートと正本から作り、一覧・構造化データ・sitemap等の同期先を確認する。
- **共通基盤変更**：共通CSS／JS、テンプレート、生成器、監査、CIへ伝播する範囲を先に列挙し、全体回帰を行う。
- **公開運用**：PR、CI、squash merge、deploy、本番確認までを一続きで行う。分析・下書き・非公開の明示がある場合だけ止める。

画面上のHotfixと仕組み改訂は原則別PRにする。再発防止が修正の成立条件で分離すると壊れる場合だけ同じPRにし、理由を残す。小変更から証拠なく全サイト調査へ広げない。

## 4. 変更種別ごとの手順

- [GitHub公開・本番確認](docs/release.md)
- [局所的なUI変更](docs/change-local-ui.md)
- [共通資産・サイト基盤の変更](docs/change-common-assets.md)
- [ブログ記事の追加・更新](docs/add-blog-post.md)
- [法令・料金・公開情報の変更](docs/change-legal-content.md)
- [日本語組版・レスポンシブの変更](docs/typography-responsive.md)

## 5. 絶対にやらないこと

- `.nw` を使うのに、隣接する文節境界の `<wbr>` を省かない。WebKitは隣接nowrapの境界に改行機会を作らない。
- `documentElement.scrollWidth` だけで合格にしない。`overflow:clip` 内の切れを拾うため要素矩形も測る。
- `content-visibility:auto` の要素をスクロールせず測らない。対象ゼロ・描画行ゼロ・実測ゼロを合格にしない。
- 共通JavaScript／CSSを変えて、同じPRで `?v=` を全参照HTMLと `admin-post.html` へ同期せず公開しない。
- `gen-sitemap.js --check` を公開ゲートにしない。本番sitemapはdeployで再生成する。
- scrollハンドラを追加式にしない。固定キーで置き換え、対象要素はハンドラ内で毎回取得し、nullをガードする。
- 助成金を「受給できます」と表現しない。事前計画提出要件と、受給保証ではない旨の注記を削らない。
- 勤務先での経験を事務所実績にしない。架空事例、根拠のないNo.1、未取得の認証・登録を載せない。
- 年末調整を事務所だけで完結する税務サービスとして扱わない。資料整理と税理士連携として扱う。
- `uploads/` に組版規則を足すとき、`wave-skin.css` への反映を忘れない。
- `.github/` をWeb本文の一括処理対象にしない。
- `public` へ直接push・force pushしない。`git reset --hard` で作業を消さない。

## 6. 完了条件

1. 最終差分が依頼範囲内で、同期対象とキャッシュ版が揃っている。
2. `node scripts/preflight.mjs` が全項目合格する。
3. 対象幅とブレークポイント前後を必要なエンジンで実測し、横はみ出し、泣き別れ、console／page errorがない。
4. 共通資産を変えた場合、本番HTMLが今回の `?v=` を実際に読み込んでいる。
5. PRの必須CI合格後にsquash mergeし、対応するdeployの成功と本番URLの内容を確認する。
6. HTMLを追加・更新・削除した場合だけ、deploy workflowが本番確認後に変更URLをIndexNowへ一度通知する。自動送信の成功後に手動で重ねて送らず、受付とインデックス登録を混同しない。

## 7. 数値はコードから取る

| 確認する数値・値 | 正本 |
|---|---|
| ブログ記事数・記事監査 | `scripts/audit-blog.mjs` |
| 公開URL数 | `gen-sitemap.js` |
| 共通資産のキャッシュ版 | `assets-version.json` |
| Lighthouse性能閾値 | `scripts/assert-lighthouse-budget.mjs` |
| NAP・公開除外・構造化データ | `scripts/check-structured-data.mjs` |
| SPAの現行版 | `page-enter.js` の `__mnSpa.v` |
| FAQ設問数と本文 | 各ページの表示FAQ |

報告値は追試できるスクリプトの出力に基づける。古い文書や記憶の固定値を転載しない。
