# 採用ページの一時非公開

`recruit.html` は採用を行わない間、公開対象外のこのフォルダに保存しています。内容は削除していません。

再公開するときは、掲載中の募集職種・待遇・応募方法を確認してからルートへ戻し、フッターの「採用情報」リンク、見出し・アクセシビリティ検査の対象を復帰させます。`node scripts/preflight.mjs` でサイトマップを再生成し、PR・CI・本番表示を確認してください。過去の文言や条件をそのまま再公開しないでください。
